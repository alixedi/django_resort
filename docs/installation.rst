============
Installation
============

At the command line::

    $ easy_install django_resort

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_resort
    $ pip install django_resort